'use strict';

module.exports = {
  async register(/*{ strapi }*/) {
    // Additional register logic can be added here if needed.
  },

  async bootstrap(/*{ strapi }*/) {
    // Application bootstrap logic can be added here if needed.
  },

  async destroy(/*{ strapi }*/) {
    // Clean-up logic can be added here if needed.
  },
};
